apiVersion: v1
kind: Service
metadata:
  name: orderprocessing-api
  namespace: orderproc
spec:
  type: ClusterIP
  selector:
    app: orderprocessing-api
  ports:
    - port: 80
      targetPort: 8080